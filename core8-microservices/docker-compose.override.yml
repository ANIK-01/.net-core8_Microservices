services:
  catalogdb:
    container_name: catalogdb
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=CatalogDb
    restart: always
    ports:
      - "6000:5432"
    volumes:
      - D:/projects_all:/var/lib/postgresql/Catalog_db

  redis_stack:
    environment:
      - REDIS_PASSWORD=
    restart: always
